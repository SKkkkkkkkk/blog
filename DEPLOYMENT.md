# 🚀 GitHub Pages 部署指南

本文档详细说明如何将你的 Hugo 博客部署到 GitHub Pages。

## 📋 前置准备

在开始之前，确保你已经：

- [x] 拥有 GitHub 账号
- [x] 已在本地完成博客搭建
- [x] 已提交所有文件到本地 Git 仓库

## 步骤 1: 在 GitHub 上创建仓库

### 1.1 登录 GitHub

访问 [GitHub](https://github.com) 并登录你的账号。

### 1.2 创建新仓库

点击右上角的 `+` 号，选择 `New repository`。

### 1.3 配置仓库

填写以下信息：

**仓库命名选项（二选一）：**

#### 选项 A：用户/组织站点（推荐）

- **Repository name**: `<你的用户名>.github.io`
  - 例如：如果你的用户名是 `zhangsan`，则填写 `zhangsan.github.io`
- **访问地址**: `https://<你的用户名>.github.io/`
- **优点**: 地址简洁，无需修改 baseURL 中的路径

#### 选项 B：项目站点

- **Repository name**: `blog`（或其他名称）
- **访问地址**: `https://<你的用户名>.github.io/blog/`
- **注意**: 需要在 `hugo.yaml` 中设置正确的 baseURL

**其他配置：**

- **Description**: （可选）"我的技术博客"
- **Public/Private**: 选择 `Public`（GitHub Pages 免费版只支持公开仓库）
- **不要勾选**：
  - ❌ Add a README file
  - ❌ Add .gitignore
  - ❌ Choose a license

点击 `Create repository`。

## 步骤 2: 修改博客配置

### 2.1 打开配置文件

```bash
cd /home/sk/blog
nano hugo.yaml  # 或使用你喜欢的编辑器
```

### 2.2 修改 baseURL

根据你选择的仓库类型修改第一行：

**如果使用选项 A（用户站点）：**

```yaml
baseURL: https://<你的用户名>.github.io/
```

**如果使用选项 B（项目站点）：**

```yaml
baseURL: https://<你的用户名>.github.io/blog/
```

**重要提示：**
- 将 `<你的用户名>` 替换为你实际的 GitHub 用户名
- baseURL 末尾的 `/` 不能省略！

### 2.3 （可选）修改个人信息

同时建议修改以下内容：

```yaml
params:
  author: 你的真实名字
  homeInfoParams:
    Title: "欢迎来到我的技术博客 👋"
    Content: >
      这里记录我的学习笔记...
  
  socialIcons:
    - name: github
      url: "https://github.com/<你的用户名>"
    - name: email
      url: "mailto:<你的邮箱>"
```

### 2.4 保存并提交更改

```bash
git add hugo.yaml
git commit -m "Update baseURL for GitHub Pages"
```

## 步骤 3: 推送到 GitHub

### 3.1 添加远程仓库

复制刚创建的 GitHub 仓库地址（在仓库页面可以看到），然后执行：

```bash
# HTTPS 方式（推荐新手）
git remote add origin https://github.com/<你的用户名>/<仓库名>.git

# 或 SSH 方式（需要配置 SSH 密钥）
git remote add origin git@github.com:<你的用户名>/<仓库名>.git
```

**示例：**

```bash
git remote add origin https://github.com/zhangsan/zhangsan.github.io.git
```

### 3.2 验证远程仓库

```bash
git remote -v
```

应该看到类似输出：

```
origin  https://github.com/zhangsan/zhangsan.github.io.git (fetch)
origin  https://github.com/zhangsan/zhangsan.github.io.git (push)
```

### 3.3 推送代码

```bash
git push -u origin main
```

如果是第一次推送，可能需要输入 GitHub 用户名和密码（或 Personal Access Token）。

## 步骤 4: 配置 GitHub Pages

### 4.1 进入仓库设置

1. 在 GitHub 仓库页面，点击 `Settings`（设置）
2. 在左侧菜单找到 `Pages`

### 4.2 配置构建源

在 "Build and deployment" 部分：

- **Source**: 选择 `GitHub Actions`
  - ⚠️ 不要选择 "Deploy from a branch"
  - 我们使用的是 Actions 自动部署

### 4.3 等待构建

1. 点击仓库顶部的 `Actions` 标签
2. 你会看到一个名为 "Deploy Hugo site to Pages" 的工作流正在运行
3. 等待几分钟，直到显示绿色的 ✓ 表示构建成功

### 4.4 访问你的博客

构建成功后，你的博客就上线了！访问地址：

- **用户站点**: `https://<你的用户名>.github.io/`
- **项目站点**: `https://<你的用户名>.github.io/<仓库名>/`

## 步骤 5: 验证部署

### 5.1 检查首页

访问博客地址，应该能看到：
- ✅ 首页正常显示
- ✅ 导航菜单正常工作
- ✅ 文章列表显示

### 5.2 测试功能

点击测试以下功能：
- 归档页面
- 搜索功能
- 文章详情页
- 代码高亮效果

### 5.3 检查样式

如果样式显示不正常（纯文本或排版错误）：

1. 确认 `baseURL` 配置正确且以 `/` 结尾
2. 检查 GitHub Actions 日志中是否有错误
3. 清除浏览器缓存后重试

## 🎉 完成！

恭喜！你的博客已经成功部署到 GitHub Pages！

## 📝 日常更新流程

以后更新博客只需要三步：

### 1. 创建或编辑文章

```bash
# 创建新文章
hugo new content/posts/my-new-article.md

# 编辑文章
nano content/posts/my-new-article.md
```

### 2. 本地预览

```bash
hugo server -D
```

访问 http://localhost:1313 确认效果。

### 3. 提交并推送

```bash
git add .
git commit -m "Add new post: 文章标题"
git push
```

GitHub Actions 会自动构建并部署，约 1-2 分钟后新内容就会上线。

## 🔧 故障排除

### 问题 1: 推送时要求输入密码

**原因**: GitHub 已不再支持密码认证。

**解决方案**: 使用 Personal Access Token (PAT)

1. 访问 GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)
2. 点击 `Generate new token (classic)`
3. 设置权限：勾选 `repo` 和 `workflow`
4. 生成后复制 token（只显示一次！）
5. 推送时使用 token 作为密码

或者配置 SSH 密钥（推荐长期使用）：

```bash
ssh-keygen -t ed25519 -C "your-email@example.com"
cat ~/.ssh/id_ed25519.pub
# 将公钥添加到 GitHub Settings → SSH and GPG keys
```

### 问题 2: Actions 构建失败

**排查步骤**:

1. 查看 Actions 标签页的错误日志
2. 常见原因：
   - 主题子模块未正确添加：`git submodule update --init --recursive`
   - `hugo.yaml` 配置错误：检查 YAML 语法
   - baseURL 配置错误：确保正确且以 `/` 结尾

### 问题 3: 页面显示 404

**可能原因**:

1. GitHub Pages 未启用：检查 Settings → Pages 设置
2. baseURL 配置错误：检查是否匹配实际访问地址
3. 构建未完成：等待 Actions 完成

### 问题 4: 样式丢失或显示不正常

**解决方法**:

1. 确认 baseURL 正确（末尾要有 `/`）
2. 清除浏览器缓存
3. 检查主题是否正确安装：`ls themes/PaperMod`
4. 强制重新构建：推送一个空提交

```bash
git commit --allow-empty -m "Trigger rebuild"
git push
```

## 📚 进阶配置

### 使用自定义域名

1. 购买域名（如 `example.com`）
2. 在域名 DNS 设置中添加 CNAME 记录：
   ```
   记录类型: CNAME
   主机记录: www
   记录值: <你的用户名>.github.io
   ```
3. 在仓库根目录创建 `static/CNAME` 文件：
   ```
   www.example.com
   ```
4. 修改 `hugo.yaml` 中的 baseURL：
   ```yaml
   baseURL: https://www.example.com/
   ```
5. 推送更改
6. 在 GitHub Settings → Pages 中会显示自定义域名状态

### 启用 HTTPS

GitHub Pages 自动提供免费 HTTPS，但首次配置自定义域名后需要：

1. 等待 DNS 传播（可能需要几小时）
2. 在 Settings → Pages 中勾选 `Enforce HTTPS`

## 🎓 总结

现在你已经掌握了：

- ✅ 在 GitHub 上创建仓库
- ✅ 配置 Hugo 博客的 baseURL
- ✅ 推送代码到 GitHub
- ✅ 配置 GitHub Pages
- ✅ 通过 GitHub Actions 自动部署
- ✅ 日常更新流程

享受写作的乐趣吧！🎉

---

**需要帮助？**

- [Hugo 官方文档](https://gohugo.io/documentation/)
- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [PaperMod 主题文档](https://github.com/adityatelange/hugo-PaperMod/wiki)

